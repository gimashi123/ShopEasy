# Promotion Service Configuration
spring.application.name=promotion-service
server.port=8085

# MongoDB
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://shopeasyuser:shopeasypassword@cluster0.sszcggs.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGO_DB:promotiondb}
spring.data.mongodb.auto-index-creation=true

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
