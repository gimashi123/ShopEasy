spring.application.name=product-service
server.port=8084

# Each service has its own MongoDB database even if the same MongoDB server is shared.
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://shopeasyuser:shopeasypassword@cluster0.sszcggs.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGO_DB:productdb}
spring.data.mongodb.auto-index-creation=true

# Must match auth-service so this service can validate the JWTs it issues.
jwt.secret=${JWT_SECRET:dGhpcy1pcy1hLXZlcnktc2VjdXJlLXNlY3JldC1rZXktZm9yLWxhdW5kcnktYXBw}

# Helpful for health checks and Docker readiness checks.
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Local product image upload settings.
app.file.upload-dir=uploads/products
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
